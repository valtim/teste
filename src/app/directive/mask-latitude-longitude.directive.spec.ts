import { MaskLatitudeLongitudeDirective } from './mask-latitude-longitude.directive';

describe('MaskLatitudeLongitudeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskLatitudeLongitudeDirective();
    expect(directive).toBeTruthy();
  });
});
