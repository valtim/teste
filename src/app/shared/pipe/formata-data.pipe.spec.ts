import { FormataDataPipe } from './formata-data.pipe';

describe('FormataDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FormataDataPipe();
    expect(pipe).toBeTruthy();
  });
});
